import { P as PasswordProtectConfig } from './handler-CFLXusx9.js';
export { F as FormOptions, h as handlePasswordProtect, l as loadConfig } from './handler-CFLXusx9.js';

/**
 * Hash a password using PBKDF2-SHA256 (Edge-compatible).
 * Returns format: pbkdf2:iterations:salt:hash (all base64url)
 */
declare function hashPassword(password: string): Promise<string>;
/**
 * Verify a password against a PBKDF2 hash string.
 * Hash format: pbkdf2:iterations:salt:hash
 */
declare function verifyPasswordHash(password: string, hashString: string): Promise<boolean>;

/**
 * Password verification and session validation.
 */

/**
 * Verify a submitted password against config.
 */
declare function verifyPassword(submitted: string, config: PasswordProtectConfig): Promise<boolean>;

export { PasswordProtectConfig, hashPassword, verifyPassword, verifyPasswordHash };
